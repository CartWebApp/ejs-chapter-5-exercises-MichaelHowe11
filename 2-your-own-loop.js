let start;
function loop1() {
  start = Date.now();
  end = 100000;
  end2 = end;
  let numList = [];

  if (end >= 1) {

    while (end >= 1) {
      numList.push(end);
      console.log(end)

      end -= 1;
    }

  }
  return (numList);
}



  function loop(num, one, two, three) {

    for (one; two; index++) {
      const element = array[index];

    }

  }